<h1> Add a new hospital on the list </h1>

<%= form_for @hospital do |f| %>
	Name: <%= f.text_field :name, placeholder: "Name" %>
	<%= f.text_field :practice_field, placeholder: "Practice Field"%>
	<%= f.text_field :location, placeholder: "Location" %>
	<%= f.text_field :benefit, placeholder: "Benefit" %>

	<%= f.submit "Add a new hospital on the list" %>

<% end %>

<%= link_to "Back to the list!", hospitals_path %>